nelmio_cors:
    defaults:
        origin_regex: true
        allow_origin: ["*"]
        allow_methods: ["GET", "OPTIONS", "POST", "PUT", "PATCH", "DELETE"]
        allow_headers:
            [
                "Content-Type",
                "Authorization",
                "Accept",
                "Origin",
                "X-Requested-With",
            ]
        expose_headers: ["*"]
        max_age: 3600
    paths:
        "^/":
            allow_origin: ["*"]
            allow_headers:
                [
                    "Content-Type",
                    "Authorization",
                    "Accept",
                    "Origin",
                    "X-Requested-With",
                ]
            allow_methods: ["POST", "PUT", "GET", "DELETE", "OPTIONS"]
            max_age: 3600
